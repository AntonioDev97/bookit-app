import React from 'react';
import { IRoom } from '@/backend/models/room.model';

interface IProps { data: IRoom }

const DatePicker = ({ data }: IProps) => {
  return (
    <div>DatePicker</div>
  )
};

export default DatePicker;
